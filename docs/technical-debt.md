# Technical debt

- DONE — The **Public Profile** feature is now backend-driven and HATEOAS-gated (change `serve-public-profile`). The backend generates the snapshot from local Session/Lap data at `GET /api/1/public-profile`, the runtime on/off toggle is persisted in the config file and flipped via `PUT /api/1/public-profile/enabled` (advertised as `publicProfileToggle`), and the `public-profile` capability link + `enabledFeatures` flag are resolved per request. The frontend `STATICALLY_ENABLED` shortcut and fake `rel` are gone; `useProfileData()` follows the index link (data-mode keyed). See `docs/public-profile.md`. **Remaining:** `distanceKm` is still an estimate (`laps × NOMINAL_LAP_KM`) — replace with real per-track length data if it becomes available; and `add-cloud-publish` should upload this snapshot rather than defining its own.

- DONE — `LapRepository.bestPerTrackQuery` (`allTimeBest=true`) now dedups at the DB level via `ROW_NUMBER() OVER (PARTITION BY track ORDER BY lap_time, id)` rather than pulling all matching rows and grouping in memory. Sort and pagination stay server-side.
- DONE — Added `GET /api/1/laps/aggregate?groupBy={track|day|week|month}` server-side `COUNT(*) GROUP BY` endpoint. `OverviewScreen` now drives **Laps per week/month** and **Tracks practiced** via this aggregate; the 1000-row `lapsQuery` is gone.
- DONE — Added `GET /api/1/sessions/aggregate?groupBy={day|week|month}` that returns per-bucket `count` and `drivingTimeMs`. `OverviewScreen` now drives the **Sessions per …**, **Driving time per …** and **Driving Time** stat card from this single request instead of reducing `sessionsQuery.items` client-side.
- DONE — The streak now derives from the all-time `GET /api/1/sessions/aggregate?groupBy=day` response (one count-only row per active day) via `streakFromDayKeys`, instead of iterating up to 100 raw session items. It's correct regardless of session count and no longer truncated by the time-range filter. `sessionsQuery` shrank to `size: 4` (only the Recent sessions list reads its items; `.total` still drives the Total Sessions card).
- DONE — `QueryExtension.firstOrNull` and `paginate` now share a `resolveOrder` helper that uses `mapNotNull`; the `!!` NPE on unknown sort fields is gone and both paths behave identically.
- DONE — `from`/`to` on lap and session search now apply a half-open interval `[from, to)`: `from` inclusive (`greaterEq`), `to` exclusive (`less`). SQL, param docs, and KDoc all agree. Boundary tests cover both repos.
- DONE — Unknown `sort` field names are still dropped (forward-compat) but now logged at DEBUG via `resolveOrder` so misuse is discoverable.
- DONE — Added ESLint 10 flat config (`frontend/eslint.config.js`) with typescript-eslint + react-hooks (classic Rules of Hooks) + react-refresh, installed the deps, fixed the `lint` script (dropped the deprecated `--ext`), and wired `pnpm lint` into the CI frontend job. Lint passes (0 errors; 13 pre-existing warnings logged). NOTE: react-hooks v7's experimental rules (purity, set-state-in-effect) flagged real findings in `OverviewScreen`/`LiveEventsProvider` — left out of the preset for now; adopting them is a separate debt item.
- DONE — Enabled `react-hooks/purity` and `react-hooks/set-state-in-effect` (errors) and fixed both findings: `OverviewScreen` now anchors finite ranges on a mount-time `useState(() => Date.now())` instead of calling `Date.now()` during render; `LiveEventsProvider` resets status to `disconnected` in the effect cleanup instead of a synchronous `setState` in the effect body. Lint, typecheck, and tests green; verified both Overview range paths render in the browser.
- DONE — Added jsdom + `@testing-library/react`. `vitest.config.ts` keeps `node` as the default env and includes `*.test.tsx`; component/hook tests opt into jsdom per-file via a `// @vitest-environment jsdom` pragma, so the node suite stays DOM-free. `LiveEventsProvider.test.tsx` now covers the render path directly: connection gating (no socket in MOCK / no live rel; connects + tracks lifecycle in LIVE; closes on unmount) and `useLiveCacheSync` wiring (LapCreated/SessionEnded invalidate the right query-key prefixes; PlayerCarUpdated invalidates nothing).

- DONE — Added browser-level **E2E tests (Playwright + Serenity/JS Screenplay)** in `frontend/e2e/`, closing the audit gap (no E2E / no Screenplay). Tests run against the production bundle (`vite preview`) in MOCK mode — deterministic, no backend — as an Actor (`Driver`) performing reusable Tasks and asking Questions (`e2e/screenplay/`), with no raw locators in spec bodies. Covers navigation across all six screens and the Sessions→detail→laps-table drill-down. A small set of `data-testid` hooks was added for ambiguous nodes. Wired a dedicated, blocking `e2e` CI job with version-keyed Playwright-browser caching and report/trace artifacts on failure. `pnpm e2e`; see `docs/frontend-technical.md` § Testing.

- `frontend/` has **two identical Vite configs** — `vite.config.js` and `vite.config.ts` — both tracked in git. Vite resolves `.js` before `.ts`, so the `.js` is the one actually loaded and the `.ts` is dead. The `__APP_VERSION__` `define` (added for the feedback form) had to be duplicated into both to keep them in sync. Delete `vite.config.js` and keep only the typed config (confirm nothing references the `.js` path).
- Feedback form is **build-time gated** (`VITE_FEEDBACK_*` are inlined by `vite build`, not read at runtime). Real values live in the committed `frontend/.env.production`, but they're currently blank, and there is **no distribution build** that actually ships the frontend yet — CI (`build-and-test.yml`) only runs `pnpm build` as a check and discards the output, and the backend doesn't serve the built assets. Action: fill in `frontend/.env.production` from the maintainer's Google Form, and wire a real release/distribution build (or have the backend serve `frontend/dist`) so downloaders get a bundle with the launcher enabled.
- Feedback form (`frontend/src/components/feedback/FeedbackButton.tsx`) submits via an unauthenticated `no-cors` Google Form POST with **no spam protection** and **no delivery confirmation** (an opaque response is assumed successful) — same limitations as the landing-page register-interest form. If abused, add a honeypot / Turnstile / Worker proxy. Wrong or edited Google Form `entry.<id>`s silently drop fields since the POST can't validate them.

- Lap telemetry linkage was **never back-filled** for laps recorded before this fix. `ClientInitializer` recorded every REALTIME_CAR_UPDATE frame with a null `lapUid`, and nothing stamped the owning lap on completion — so `findByLapUid` returned nothing and every lap comparison / telemetry trace was empty. Fixed going forward: on `LAPCOMPLETED`, `LinkLapTelemetryService.linkCompletedLap` stamps the new lap's id/uid onto that car's still-unlinked frames (`sessionUid + carIndex + laps < lapNumber`). **Remaining debt:** existing rows in already-captured databases still have null `lap_uid` and won't show telemetry — needs a one-time migration that links historical frames to laps by `(session_id, car_index, laps → lap_number)`. Until then, only laps recorded after the fix render telemetry.
- Missing-car on laps is now corrected going forward: a lap that completes before its `EntryListCar` is processed is recorded with a null `car`, and `RecordCarOnLapsService.fillMissingCar` now back-fills it when the model is registered (`UPDATE LAP SET car WHERE session_uid + car_id AND car IS NULL`), plus the `EntryListCar` registration is no longer gated on the focused car being known. **Remaining debt:** laps already stored with a null `car` in existing databases aren't repaired — needs a one-time migration (e.g. re-derive from the car's other laps in the same session, or from retained `EntryListCar` data) to fill historical null `car` values.

## Landing page (landing/)

- Landing page screenshot (`landing/assets/dashboard-preview.svg`) and social share image (`landing/assets/og-image.svg`) are placeholder SVGs. Replace with real PNG exports of the dashboard, and re-capture the screenshot whenever the dashboard UI changes meaningfully so the landing page stays accurate.
- Cloudflare Pages project still needs to be created/connected in the Cloudflare dashboard (root dir `landing`, build `pnpm install && pnpm build`, output `dist`), plus DNS: apex `laptimeinsights.com` → Pages. (The app is self-hosted — there is no hosted app subdomain; CTAs link to GitHub releases.) This is a one-time manual infra step — see `landing/README.md`.
- If the landing site grows beyond a single page, migrate from hand-written HTML to Astro or Vite to avoid duplicated markup (out of scope for the initial change).
- Register-interest form (`#register-interest` in `landing/index.html`) has **no spam protection** — it's a public, unauthenticated Google Form POST. If abused pre-launch, add a honeypot field, Cloudflare Turnstile, or a Worker proxy. It also can't confirm Google actually recorded the row: the `no-cors` POST returns an opaque response, so a resolved request is *assumed* successful — the linked Google Sheet is the source of truth. A Worker proxy would give real status. Also: the Google Form `FORM_ACTION` / `EMAIL_ENTRY` placeholders in `index.html` must be set before deploy (see `landing/README.md`).
