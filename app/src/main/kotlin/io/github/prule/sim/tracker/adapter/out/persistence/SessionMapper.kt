package io.github.prule.sim.tracker.adapter.out.persistence

import io.github.prule.sim.tracker.application.domain.model.Session

class SessionMapper {
    fun toDomain(entity: SessionEntity): Session {
    }

    fun toEntity(session: Session) {
    }
}
